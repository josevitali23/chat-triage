<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Video Consulta Médica</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/feather-icons/dist/feather.min.js"></script>
</head>
<body class="bg-gradient-to-br from-blue-100 to-blue-200 flex items-center justify-center min-h-screen">
    <div class="bg-white p-8 rounded-2xl shadow-2xl w-full max-w-lg">
        <div class="text-center mb-6">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-16 w-16 mx-auto text-blue-600 mb-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 10l4.553-2.276A1 1 0 0121 8.618v6.764a1 1 0 01-1.447.894L15 14M5 18h8a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v8a2 2 0 002 2z" />
            </svg>
            <h1 class="text-2xl font-bold text-blue-800">Video Consulta en Espera</h1>
            <p class="text-gray-600 mt-2">Un médico se conectará contigo próximamente</p>
        </div>

        <div class="bg-blue-50 p-4 rounded-lg mb-6">
            <div class="flex items-center mb-4">
                <i data-feather="clock" class="text-blue-600 mr-3"></i>
                <span class="font-semibold text-blue-800">Tiempo de Espera Estimado: 10-15 minutos</span>
            </div>
            <div class="flex items-center">
                <i data-feather="info" class="text-blue-600 mr-3"></i>
                <span class="text-gray-700">Mantén tu dispositivo preparado con conexión estable a internet</span>
            </div>
        </div>

        <div class="text-center">
            <div class="inline-block bg-blue-100 p-4 rounded-lg mb-4">
                <h2 class="text-xl font-semibold text-blue-900 mb-2">Preparación para la Consulta</h2>
                <ul class="text-left text-gray-700 space-y-2">
                    <li class="flex items-center">
                        <i data-feather="check-circle" class="text-green-500 mr-2"></i>
                        Busca un lugar tranquilo y bien iluminado
                    </li>
                    <li class="flex items-center">
                        <i data-feather="check-circle" class="text-green-500 mr-2"></i>
                        Verifica tu cámara y micrófono
                    </li>
                    <li class="flex items-center">
                        <i data-feather="check-circle" class="text-green-500 mr-2"></i>
                        Ten a mano tu información médica
                    </li>
                </ul>
            </div>
        </div>

        <div class="mt-6 text-center">
            <div class="inline-flex items-center bg-green-100 text-green-800 p-3 rounded-lg">
                <i data-feather="wifi" class="mr-2"></i>
                <span class="font-medium">Conexión lista para video consulta</span>
            </div>
        </div>
    </div>

    <script>
        feather.replace(); // Initialize Feather icons
        
        // Optional: Add a countdown timer
        const estimatedWaitTime = 15 * 60; // 15 minutes in seconds
        const timerDisplay = document.createElement('div');
        timerDisplay.className = 'fixed bottom-4 right-4 bg-white p-3 rounded-lg shadow-lg';
        document.body.appendChild(timerDisplay);

        function updateCountdown() {
            const minutes = Math.floor(estimatedWaitTime / 60);
            const seconds = estimatedWaitTime % 60;
            timerDisplay.textContent = `Tiempo restante: ${minutes}:${seconds < 10 ? '0' : ''}${seconds}`;
            
            if (estimatedWaitTime > 0) {
                estimatedWaitTime--;
                setTimeout(updateCountdown, 1000);
            }
        }

        updateCountdown();
    </script>
</body>
</html>